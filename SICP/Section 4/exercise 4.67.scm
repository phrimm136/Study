#lang sicp
; A pair of pattern and variable should be settled in the histroy.
; When a procedure is about to apply, it should be checked whether it's duplicate.
; If so, it aborts. Otherwise, it is added to the history and keeps applying.


; Need to implement it after section 4.4.4.