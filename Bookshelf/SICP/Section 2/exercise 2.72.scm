#lang sicp
; The encoding procedure in ex 2.68 scans all cases without frequency of a symbol.
; Searching all branches: O(n), Checking all leaves: O(n) => O(n)

; Encoding the most frequent symbol in the alphabet: O(n)
; Encoding the least frequent symbol in the alphabet: O(n)