Q.

Suppose $T$ is a table of ten numbers with $T[i] = i$ for $0 \le i \le 9$.
What are the values of $a$, $b$, and $middle$ on successive calls to
$BinarySearch$ (Algorithm 1.3) starting with $BinarySearch(T[0..9], 3)$?

A.

a  b  middle
0  9    4
0  3    1
2  3    2
3  3    3
