#=

Q.

Show that the expected maximum level in a skip list is at most
log_(1/p) n - 1 + 1 /(1 - p),
where p < 1 is the ratio with which nodes of successive levels are expected to occur.

A.

If we have p, then the (1/p)^k nodes have k height. 1/p^m = n <=> m = log_(1/p) n.
