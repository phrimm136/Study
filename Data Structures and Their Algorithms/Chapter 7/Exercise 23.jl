#=

Q.

Why does the method of "anticipatory splitting" of B-tree nodes described
on page 242 not work with b = 2a - 1?

A.

Because splitting a node with b=2a-1 violates the rule of (a,b)-tree; every node has at least a childs.

=#
