#=

Q.

Given n < 2^m numbers of m bits each, show how to find a num-
ber different from all of them in time O(n), on the hypothesis that
comparing two m-bit numbers can be done in constant time.

A.

Utilize the first step of exemple 24, without flooring bucket number,
then find keys which have one element.
The resulting elements are distinct from all other elements for each.

=#
