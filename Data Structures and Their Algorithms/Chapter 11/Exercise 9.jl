#=

Q.

How does Heap Sort behave if the table is in order already? in reverse
order?

A.

Initializing heap makes ordered table to smallest heap (rightmost).
With reverse ordered table, initializing has no effect.
Since two tables is heapified, the lterative heapifying behaves as normal.

=#
