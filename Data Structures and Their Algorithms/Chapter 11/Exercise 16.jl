#=

Q.

In Algorithm 11.6 on page 390 one of the two tests in the inner loops,
"i ≤ r" and "j ≥ l", is unnecessary. Which one, and why?

A.

Since if j = l then A[j] = v = A[l], j is always larger than or
equal to l, so j ≥ l is unnecessary.

=#
